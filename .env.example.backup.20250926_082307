# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/fanz_db

# PostgreSQL Connection Details (Individual)
PGHOST=localhost
PGPORT=5432
PGDATABASE=fanz_db
PGUSER=username
PGPASSWORD=password

# Object Storage Configuration
PUBLIC_OBJECT_SEARCH_PATHS=/bucket-name/public-assets
PRIVATE_OBJECT_DIR=/bucket-name/private

# JWT Configuration
JWT_ISS=fun.app
JWT_AUD=fun.clients
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=2592000
JWT_SECRET=your-jwt-secret-key-64-characters-long

# Session Configuration
SESSION_SECRET=your-session-secret-key

# Application URLs
WEB_APP_URL=http://localhost:5000
API_URL=http://localhost:5000/api

# Environment
NODE_ENV=development

# Email Configuration
OTP_EMAIL_FROM=noreply@yourdomain.com

# KYC/VerifyMy Integration
VERIFYMY_API_URL=https://api.verifymy.com
VERIFYMY_API_KEY=your-verifymy-api-key

# Replit Configuration (Auto-provided in Replit environment)
REPL_ID=your-repl-id
REPLIT_DOMAINS=your-repl-domain.repl.co
ISSUER_URL=https://replit.com/oidc

# Port Configuration
PORT=5000
